{
  "extends": [
    "next/core-web-vitals",
    "next/typescript"
  ],
  // 自定义规则，让ts-eslint 不对未使用的变量报错
  "rules": {
    "@typescript-eslint/no-unused-vars": ["warn", { "vars": "all", "args": "after-used", "ignoreRestSiblings": true }]
  }
}
